[package]
name = "tauri-plugin-studytools"
version = "0.1.0"
edition = "2021"
links = "tauri-plugin-studytools"

[dependencies]
tauri = { version = "2", default-features = false }

[build-dependencies]
tauri-plugin = { version = "2", features = ["build"] }
